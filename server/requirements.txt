fastapi
uvicorn[standard]
anthropic[bedrock]
python-dotenv
python-multipart
pydantic
pytest
# fastapi.testclient transport. Pulled in by anthropic today, but the route tests
# import it directly, so it is named rather than left as somebody else's transitive.
httpx
