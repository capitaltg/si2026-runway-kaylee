# Fixtura "Runway funding-pace demo" data set

The contract that must NOT cry wolf: amber. Its funded dollars run dry inside FAR 52.232-22(c)'s 60-day notification window, but the ceiling holds and the obligations are landing as fast as the dollars burn — so it reads 'Funding due', never red.

Generated by Fixtura (si2026-test-generator). One coherent contract across every
file — the same seed ties them together.

**Regenerate with `python3 sample-data/regenerate.py`.** Do not hand-edit the
numbers below; they are measured against Runway's burn engine at generation time
and rewritten by that script. See "Staleness" at the bottom.

## Files

| File | What it is |
|---|---|
| `fixtura-runway-funding-pace-demo.award.sf26.pdf` | The signed award on a real **SF-26**, plus the Section B fully-burdened labor-rate schedule. Drop into Runway's Ingest step. |
| `fixtura-runway-funding-pace-demo.mod.P00001.sf30.pdf` | **SF-30** modification P00001 — feed to `POST /api/contracts/{id}/mods`, one at a time. |
| `fixtura-runway-funding-pace-demo.mod.P00002.sf30.pdf` | **SF-30** modification P00002 — feed to `POST /api/contracts/{id}/mods`, one at a time. |
| `fixtura-runway-funding-pace-demo.contract.json` | The award as structured data — periods, CLINs, labor rates, ceiling vs. obligated, and the full `obligation_history`. |
| `fixtura-runway-funding-pace-demo.timesheets.csv` | Weekly hours booked to the labor CLINs — 180 rows, one per person per week across the elapsed PoP. `total_hours` is **billable** (regular + overtime); leave and holidays are carried separately and are not chargeable. |
| `fixtura-runway-funding-pace-demo.labor.csv` | Labor distribution sample (bill rate x hours), illustrative only — Runway burns from the timesheets. |
| `fixtura-runway-funding-pace-demo.planned-leave.csv` | Dated FUTURE absence for the same roster — the input a what-if projection needs, which the timesheets cannot provide. |

## The award

PIID **7025GMLQJC0000818** (Department of Homeland Security), T&M. Ceiling
**$2,224,860.80**, obligated **$1,284,000.00** (57.7% funded). PoP
**2025-11-26 -> 2026-11-25** — mid-flight at week
**36 of 52**, 16 weeks remaining.

CLINs: 0001 (labor), 0002 (cost), 0003 (cost).

## The obligation history (what the SF-30s rebuild)

The SF-26 carries only the **initial** obligation. Funding pace comes from the
mods — ingest them one at a time to reconstruct the dated history:

| Action | Effective | Amount | Cumulative obligated | Type |
|---|---|---|---|---|
| Award | 2025-11-26 | $500,000.00 | $500,000.00 | Initial award / base-period funding |
| P00001 | 2026-02-18 | $310,000.00 | $810,000.00 | Incremental funding (FAR 52.232-22) |
| P00002 | 2026-05-13 | $474,000.00 | $1,284,000.00 | Incremental funding (FAR 52.232-22) |

Re-ingesting a mod is idempotent (dedup by mod number).

## Measured result

Measured by `burn.compute` on the generated timesheets, 2026-08-05:

| CLIN | Title | Ceiling | Funded | Funded % | Spent | Funds exhaust | Runway | Status |
|---|---|---|---|---|---|---|---|---|
| 0001 | Professional Services (Labor) | $2,090,860.80 | $1,250,000.00 | 59.8% | $1,100,431.92 | wk 41.89 | 41 d | `funding` |

Red tripwires firing: **0**. Contract `all_clear`: **False**.

**Acceptance (60-day gate):** PASS — 1 of 1 labor CLIN(s) inside FAR 52.232-22(c)'s notification lookahead. This is the bar, not `funded_frac < elapsed_frac`: funding lagging the clock is necessary but not sufficient, and a CLIN 74 days out reads the ceiling story no matter how far behind its obligations are.

## How it was generated

Fixtura preset generation, **seed 19**, with:

```json
{
  "pop_in_progress": true,
  "option_years": 0,
  "contract_type": "T&M",
  "funding": "incremental",
  "staffing": 0.75,
  "target_hours": 35
}
```

`staffing` and `target_hours` apply only to the labor exports — the award itself
is identical without them. The award is `govcon_award_sf26`; the structured data
is `govcon_contract_data`; the SF-30s are `govcon_mod_sf30` with `split_mods`.

## Staleness

This bundle is `pop_in_progress`, which anchors the period of performance to
**today**. The committed CSVs therefore age: the timesheets stop at the week they
were generated, while the clock keeps moving, so a bundle built at week 36 of 52
is read against week 40 a month later and every figure above drifts. Runway's
amber gate is a *runway-days* threshold, so drift moves the verdict, not just the
decimals.

Regenerate rather than trust a stale bundle:

```
python3 sample-data/regenerate.py funding-pace
```
